{
  "name": "tenthproject-monorepo",
  "private": true,
  "workspaces": ["Tenthproject"],
  "dependencies": {
    "next": "14.2.18",
    "react": "^18",
    "react-dom": "^18"
  },
  "scripts": {
    "vercel-build": "npm run build",
    "verify:vercel-manifest": "node scripts/verify-vercel-manifest.mjs",
    "build": "npm run build -w tenthproject && npm run stage:vercel",
    "stage:vercel": "rm -rf .next && cp -a Tenthproject/.next .next && cp Tenthproject/next.config.mjs next.config.mjs && (test -d Tenthproject/public && rm -rf public && cp -a Tenthproject/public public || true)",
    "lint": "npm run lint -w tenthproject",
    "dev:web": "npm run dev -w tenthproject"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
